# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index()

# Hello action
GET     /hello                      controllers.Application.sayHello()

# Workflow page
GET     /workflow                   controllers.Application.workflow()
GET     /wormsworkflow                   controllers.Application.wormsworkflow()

# Workflow run
GET     /run                   controllers.Application.run()
POST     /runworms                   controllers.Application.runworms()

# Workflow result
GET     /result/*file                 controllers.Application.result(file)

# Javascript routes
GET   /jsRoutes                controllers.Application.jsRoutes()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
